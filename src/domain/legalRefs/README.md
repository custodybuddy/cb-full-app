# Legal References

This module consolidates legal reference data and helper utilities.

- `data.ts`: Static references, mappings, and jurisdiction-aware lookup helpers.
- `utils.ts`: Parsing/formatting helpers for extracting references from text.

When rebuilding logic, keep data in `data.ts` and avoid mixing in network calls.
